<div class="issue-column">
    <div class="issue-column-header">
        <span class="issue-column-title">{{ title }}</span>
        <span class="issue-count-badge">{{ issues|length }}</span>
    </div>
    <div class="issue-column-body">
        {% for issue in issues %}
        <div class="issue-row">
            <div class="issue-row-top">
                <span class="issue-id">#{{ issue.id }}</span>
                <span class="priority-badge priority-{{ issue.priority }}">{{ issue.priority }}</span>
            </div>
            <div class="issue-summary">{{ issue.summary }}</div>
            <div class="issue-row-bottom">
                <span class="issue-category">{{ issue.category.name }}</span>
                <span class="issue-date">{{ issue.submittedAt|date('M j, Y') }}</span>
            </div>
        </div>
        {% else %}
        <div class="issue-empty">No issues here yet.</div>
        {% endfor %}
    </div>
</div>
